{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nature</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nature.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nature.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('nature_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('nature_edit', { 'id': nature.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
