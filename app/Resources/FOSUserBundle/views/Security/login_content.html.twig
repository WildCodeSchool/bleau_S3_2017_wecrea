{% trans_default_domain 'FOSUserBundle' %}

<div class="container">
    <div class="row">
        <div class="col m6 offset-m3 s12">
            {% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <form action="{{ path("fos_user_security_check") }}" method="post">
                {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                {% endif %}
                <div class="input-field">
                    <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" />
                    <label for="username">{{ 'security.login.username'|trans }}</label>
                </div>

                <div class="input-field">
                    <input type="password" id="password" name="_password" required="required" />
                    <label for="password">{{ 'security.login.password'|trans }}</label>
                    <a href="{{ path('fos_user_resetting_request') }}">Mot de pass oublié</a>
                </div>

                <div class="input-field row center-align">
                    <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                    <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                </div>

                <div class="input-field row">
                    <button class="btn waves-effect waves-light connect_button" id="_submit" type="submit" name="_submit">
                        {{ 'security.login.submit'|trans }}
                        <i class="material-icons left">send</i>
                    </button>
                </div>

                <div class="input-field row">
                    <a class="btn waves-effect waves-light connect_button" href="{{ path('fos_user_registration_register') }}">
                        Créer un compte
                        <i class="material-icons left">send</i>
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
