{% extends '@WeCrea/layout/base_admin.html.twig' %}

{% block body %}
<div class="container container-custom-artists">
    <h4 style="background: #f2da00; color: #fff; padding: 10px;"> Édition du carrousel </h4>
    <div class="row">
        <div class="col s9 offset-s2">
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}